version: "3.8"

services:
  blog-backend:
    build: .
    container_name: blog-backend
    restart: always

    ports:
      - "3000:3000"

    environment:
      NODE_ENV: production
      PORT: 3000
