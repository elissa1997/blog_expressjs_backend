const CONTENT_STATUS = Object.freeze({
  PASS: 'pass',
  HIDE: 'hide'
});

const CONTENT_STATUS_VALUES = Object.freeze(Object.values(CONTENT_STATUS));

module.exports = {
  CONTENT_STATUS,
  CONTENT_STATUS_VALUES
};
